version: 2

workflows:
  single-node:
    steps:
      -
        name: "test-nvidia"
        command: experiment.run_single_node
        params:
          command: bash run.sh
          container: pytorchlightning/pytorch_lightning:nightly-py3.7-torch1.5
          machineType: "K80"
