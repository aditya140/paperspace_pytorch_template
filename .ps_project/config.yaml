version: 2

workflows:
  single-node:
    steps:
      -
        name: "test-nvidia"
        command: experiment.run_single_node
        params:
          command: bash run.sh
          container: paperspace/dl-containers:pytorch-py36-cu100-jupyter
          machineType: "K80"
